// LOGICAL OPERATORS

// OR ||
// AND &&

const password = "p@ssword";

if (password.length >= 8 && password.includes("@")) {
  console.log("great password");
} else {
  console.log("please improve password");
}

// NOTES:
/*
- AND && if something is this AND that then code block will fire
- OR || if something is this OR that but only one has to pass for the code block to fire
*/
